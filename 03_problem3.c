// write a C program to find the VOLUME of CYCLINDER

#include <stdio.h>

int main()
{
    int r = 6;
    int height = 5;
    printf("The volume of cyclinder with radius %d and height %d is %f", r, height, 3.14 * r * r * height);
    return 0;
}